-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.call_order (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text,
  birthday text,
  plan text,
  freebie text,
  installment text,
  discount text,
  benefit text,
  petName text,
  subPhone text,
  totalMonthPayment text,
  carrier text,
  is_processed boolean DEFAULT false,
  CONSTRAINT call_order_pkey PRIMARY KEY (no)
);
CREATE TABLE public.consultation_requests (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name text NOT NULL,
  phone text NOT NULL,
  birth_date date NOT NULL,
  is_processed boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT consultation_requests_pkey PRIMARY KEY (id)
);
CREATE TABLE public.consultation_rrn (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  name text NOT NULL,
  phone text NOT NULL,
  rrn text NOT NULL,
  carrier text,
  is_processed boolean NOT NULL DEFAULT false,
  birthday text,
  CONSTRAINT consultation_rrn_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customer_consultations (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  name text NOT NULL,
  phone text NOT NULL,
  birthday text NOT NULL,
  description text,
  mobile_carrier text,
  is_processed boolean DEFAULT false,
  CONSTRAINT customer_consultations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customer_survey (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  name text NOT NULL,
  phone text NOT NULL,
  birth text NOT NULL,
  address_dong text NOT NULL,
  target_model text NOT NULL,
  purchase_period text NOT NULL,
  purchase_plan text,
  monthly_fee text NOT NULL,
  internet_tv text,
  family_skt text,
  preferences ARRAY,
  memo text,
  is_processed boolean NOT NULL DEFAULT false,
  current_carrier text NOT NULL DEFAULT '미입력'::text,
  CONSTRAINT customer_survey_pkey PRIMARY KEY (id)
);
CREATE TABLE public.device_plans_chg (
  id uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  pet_name text NOT NULL,
  model text,
  model_price bigint,
  plan_id text,
  name text,
  disclosure_subsidy bigint,
  price bigint,
  CONSTRAINT device_plans_chg_pkey PRIMARY KEY (id),
  CONSTRAINT device_plans_chg_model_fkey FOREIGN KEY (model) REFERENCES public.devices(model)
);
CREATE TABLE public.device_plans_chg_duplicate (
  id uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  pet_name text NOT NULL,
  model text,
  model_price bigint,
  plan_id text,
  name text,
  disclosure_subsidy bigint,
  price bigint,
  CONSTRAINT device_plans_chg_duplicate_pkey PRIMARY KEY (id),
  CONSTRAINT device_plans_chg_duplicate_model_fkey FOREIGN KEY (model) REFERENCES public.devices(model)
);
CREATE TABLE public.device_plans_mnp (
  id uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  pet_name text NOT NULL,
  model text,
  model_price bigint,
  plan_id text,
  name text,
  disclosure_subsidy bigint,
  price bigint,
  CONSTRAINT device_plans_mnp_pkey PRIMARY KEY (id),
  CONSTRAINT device_plans_mnp_model_fkey FOREIGN KEY (model) REFERENCES public.devices(model)
);
CREATE TABLE public.device_plans_mnp_duplicate (
  id uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  pet_name text NOT NULL,
  model text,
  model_price bigint,
  plan_id text,
  name text,
  disclosure_subsidy bigint,
  price bigint,
  CONSTRAINT device_plans_mnp_duplicate_pkey PRIMARY KEY (id),
  CONSTRAINT device_plans_mnp_duplicate_model_fkey FOREIGN KEY (model) REFERENCES public.devices(model)
);
CREATE TABLE public.device_plans_new (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  pet_name text NOT NULL,
  model text,
  model_price bigint,
  plan_id text,
  name text,
  disclosure_subsidy bigint,
  price bigint,
  CONSTRAINT device_plans_new_pkey PRIMARY KEY (id),
  CONSTRAINT device_plans_new_model_fkey FOREIGN KEY (model) REFERENCES public.devices(model)
);
CREATE TABLE public.devices (
  category text,
  model text NOT NULL,
  pet_name text,
  colors_kr ARRAY NOT NULL DEFAULT '{}'::text[],
  colors_en ARRAY NOT NULL DEFAULT '{}'::text[],
  capacities ARRAY NOT NULL DEFAULT '{}'::text[],
  paths ARRAY NOT NULL DEFAULT '{}'::text[],
  images jsonb DEFAULT '{}'::jsonb,
  colors_code ARRAY NOT NULL,
  company text,
  capacity text,
  network_type text,
  form_link text,
  subsidy integer,
  category_kr text,
  video_link text,
  plan_id text,
  is_available boolean,
  price integer,
  priority smallint,
  thumbnail text,
  release_date date,
  CONSTRAINT devices_pkey PRIMARY KEY (model)
);
CREATE TABLE public.devices_duplicate (
  category text,
  model text NOT NULL,
  pet_name text,
  colors_kr ARRAY NOT NULL DEFAULT '{}'::text[],
  colors_en ARRAY NOT NULL DEFAULT '{}'::text[],
  capacities ARRAY NOT NULL DEFAULT '{}'::text[],
  paths ARRAY NOT NULL DEFAULT '{}'::text[],
  images jsonb DEFAULT '{}'::jsonb,
  colors_code ARRAY NOT NULL,
  company text,
  capacity text,
  network_type text,
  form_link text,
  subsidy integer,
  category_kr text,
  video_link text,
  plan_id text,
  is_available boolean,
  price integer,
  priority smallint,
  thumbnail text,
  release_date date,
  CONSTRAINT devices_duplicate_pkey PRIMARY KEY (model)
);
CREATE TABLE public.freebies (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  sort integer,
  title text,
  origin_price bigint DEFAULT '0'::bigint,
  discount_price bigint DEFAULT '0'::bigint,
  installment integer DEFAULT 24,
  monthly_price bigint DEFAULT '0'::bigint,
  plans ARRAY NOT NULL,
  is_available boolean DEFAULT true,
  CONSTRAINT freebies_pkey PRIMARY KEY (no)
);
CREATE TABLE public.freebies_duplicate (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  sort integer,
  title text,
  origin_price bigint DEFAULT '0'::bigint,
  discount_price bigint DEFAULT '0'::bigint,
  installment integer DEFAULT 24,
  monthly_price bigint DEFAULT '0'::bigint,
  plans ARRAY NOT NULL,
  is_available boolean DEFAULT true,
  CONSTRAINT freebies_duplicate_pkey PRIMARY KEY (no)
);
CREATE TABLE public.gonggu-asamo (
  id bigint NOT NULL DEFAULT nextval('"gonggu-asamo_id_seq"'::regclass),
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  company text,
  device text,
  capacity text,
  color text,
  name text,
  birthday text,
  phone text,
  funnel text,
  is_agreed_tos boolean,
  form_data jsonb,
  is_processed boolean DEFAULT false,
  CONSTRAINT gonggu-asamo_pkey PRIMARY KEY (id)
);
CREATE TABLE public.iphone17_order (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  capacity text,
  color text,
  pet_name text,
  name text,
  phone text,
  birthday text,
  register text,
  plan text,
  installment text,
  model text,
  discount text,
  freebie text,
  freebie_second text,
  benefit text,
  form_link text,
  is_guaranteed_return boolean,
  guardian text,
  guardian_birthday text,
  guardian_phone text,
  installment_principal text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  carrier text,
  profile_id uuid,
  kakao_friend_rewarded boolean DEFAULT false,
  kakao_friend_checked_at timestamp with time zone,
  CONSTRAINT iphone17_order_pkey PRIMARY KEY (no),
  CONSTRAINT iphone17_order_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.kakao_channel_status (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  profile_id uuid NOT NULL UNIQUE,
  kakao_user_id text NOT NULL UNIQUE,
  relation text NOT NULL,
  channel_added_at timestamp with time zone,
  channel_updated_at timestamp with time zone,
  last_checked_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT kakao_channel_status_pkey PRIMARY KEY (id),
  CONSTRAINT kakao_channel_status_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.ktmarket_subsidy (
  network text,
  model text NOT NULL,
  company text,
  plan_discount_new_gte_110000 bigint,
  plan_discount_mnp_gte_110000 bigint,
  plan_discount_chg_gte_110000 bigint,
  plan_discount_new_gte_100000 bigint,
  plan_discount_mnp_gte_100000 bigint,
  plan_discount_chg_gte_100000 bigint,
  plan_discount_new_gte_61000 bigint,
  plan_discount_mnp_gte_61000 bigint,
  plan_discount_chg_gte_61000 bigint,
  plan_discount_new_gte_37000 bigint,
  plan_discount_mnp_gte_37000 bigint,
  plan_discount_chg_gte_37000 bigint,
  plan_discount_new_lt_37000 bigint,
  plan_discount_mnp_lt_37000 bigint,
  plan_discount_chg_lt_37000 bigint,
  device_discount_new_gte_110000 bigint,
  device_discount_mnp_gte_110000 bigint,
  device_discount_chg_gte_110000 bigint,
  device_discount_new_gte_100000 bigint,
  device_discount_mnp_gte_100000 bigint,
  device_discount_chg_gte_100000 bigint,
  device_discount_new_gte_61000 bigint,
  device_discount_mnp_gte_61000 bigint,
  device_discount_chg_gte_61000 bigint,
  device_discount_new_gte_37000 bigint,
  device_discount_mnp_gte_37000 bigint,
  device_discount_chg_gte_37000 bigint,
  device_discount_new_lt_37000 bigint,
  device_discount_mnp_lt_37000 bigint,
  device_discount_chg_lt_37000 bigint,
  plan_discount_new_gte_90000 bigint,
  plan_discount_mnp_gte_90000 bigint,
  plan_discount_chg_gte_90000 bigint,
  device_discount_new_gte_90000 bigint,
  device_discount_mnp_gte_90000 bigint,
  device_discount_chg_gte_90000 bigint,
  CONSTRAINT ktmarket_subsidy_pkey PRIMARY KEY (model)
);
CREATE TABLE public.ktmarket_subsidy_duplicate (
  network text,
  model text NOT NULL,
  company text,
  plan_discount_new_gte_110000 bigint,
  plan_discount_mnp_gte_110000 bigint,
  plan_discount_chg_gte_110000 bigint,
  plan_discount_new_gte_100000 bigint,
  plan_discount_mnp_gte_100000 bigint,
  plan_discount_chg_gte_100000 bigint,
  plan_discount_new_gte_61000 bigint,
  plan_discount_mnp_gte_61000 bigint,
  plan_discount_chg_gte_61000 bigint,
  plan_discount_new_gte_37000 bigint,
  plan_discount_mnp_gte_37000 bigint,
  plan_discount_chg_gte_37000 bigint,
  plan_discount_new_lt_37000 bigint,
  plan_discount_mnp_lt_37000 bigint,
  plan_discount_chg_lt_37000 bigint,
  device_discount_new_gte_110000 bigint,
  device_discount_mnp_gte_110000 bigint,
  device_discount_chg_gte_110000 bigint,
  device_discount_new_gte_100000 bigint,
  device_discount_mnp_gte_100000 bigint,
  device_discount_chg_gte_100000 bigint,
  device_discount_new_gte_61000 bigint,
  device_discount_mnp_gte_61000 bigint,
  device_discount_chg_gte_61000 bigint,
  device_discount_new_gte_37000 bigint,
  device_discount_mnp_gte_37000 bigint,
  device_discount_chg_gte_37000 bigint,
  device_discount_new_lt_37000 bigint,
  device_discount_mnp_lt_37000 bigint,
  device_discount_chg_lt_37000 bigint,
  plan_discount_new_gte_90000 bigint,
  plan_discount_mnp_gte_90000 bigint,
  plan_discount_chg_gte_90000 bigint,
  device_discount_new_gte_90000 bigint,
  device_discount_mnp_gte_90000 bigint,
  device_discount_chg_gte_90000 bigint,
  CONSTRAINT ktmarket_subsidy_duplicate_pkey PRIMARY KEY (model)
);
CREATE TABLE public.link_product (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone DEFAULT (now() + '09:00:00'::interval),
  name text NOT NULL,
  phone text NOT NULL,
  birthday text,
  selections jsonb,
  marketing_agreed boolean DEFAULT false,
  privacy_agreed boolean DEFAULT false,
  is_processed boolean NOT NULL DEFAULT false,
  CONSTRAINT link_product_pkey PRIMARY KEY (id)
);
CREATE TABLE public.official_form_errors (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  phone text NOT NULL,
  birthday text NOT NULL,
  error_details text,
  is_processed boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  discount_type text,
  CONSTRAINT official_form_errors_pkey PRIMARY KEY (id)
);
CREATE TABLE public.online_order (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text,
  birthday text,
  plan text,
  freebie text,
  installment text,
  model text,
  discount text,
  benefit text,
  petName text,
  installment_principal text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  carrier text,
  profile_id uuid,
  kakao_friend_rewarded boolean DEFAULT false,
  kakao_friend_checked_at timestamp with time zone,
  CONSTRAINT online_order_pkey PRIMARY KEY (no),
  CONSTRAINT online_order_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.preorder-galaxy26 (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  phone character varying NOT NULL,
  birthday text,
  emergency_contact character varying,
  description text,
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  updated_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  mobile_carrier text,
  model text,
  is_processed boolean DEFAULT false,
  funnel text,
  color text,
  is_colors boolean DEFAULT false,
  CONSTRAINT preorder-galaxy26_pkey PRIMARY KEY (id)
);
CREATE TABLE public.preorder-galaxy26-other (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  phone character varying NOT NULL,
  birthday text,
  emergency_contact character varying,
  description text,
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  updated_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  mobile_carrier text,
  model text,
  is_processed boolean DEFAULT false,
  funnel text,
  color text,
  is_colors boolean DEFAULT false,
  country text,
  CONSTRAINT preorder-galaxy26-other_pkey PRIMARY KEY (id)
);
CREATE TABLE public.preorder_17e_orders (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  model text,
  petName text,
  capacity text,
  color text,
  name text,
  phone text UNIQUE,
  birthday text,
  register text,
  plan text,
  discount text,
  installment text,
  installment_principal text,
  freebie text,
  benefit text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  carrier text,
  profile_id uuid,
  kakao_friend_rewarded boolean DEFAULT false,
  kakao_friend_checked_at timestamp with time zone,
  CONSTRAINT preorder_17e_orders_pkey PRIMARY KEY (no),
  CONSTRAINT preorder_17e_orders_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.preorder_a37 (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  phone character varying NOT NULL,
  birthday text,
  emergency_contact character varying,
  description text,
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  updated_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  mobile_carrier text,
  model text,
  is_processed boolean DEFAULT false,
  funnel text,
  color text,
  is_colors boolean DEFAULT false,
  CONSTRAINT preorder_a37_pkey PRIMARY KEY (id)
);
CREATE TABLE public.preorder_foldable7 (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text,
  funnel text,
  birthday text,
  plan text,
  freebie text,
  installment smallint,
  CONSTRAINT preorder_foldable7_pkey PRIMARY KEY (no)
);
CREATE TABLE public.preorder_iPhone17 (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  register text,
  name text,
  phone text,
  carrier text,
  CONSTRAINT preorder_iPhone17_pkey PRIMARY KEY (no)
);
CREATE TABLE public.preorder_iphone17e (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  model text DEFAULT 'iPhone 17e'::text,
  name text NOT NULL,
  phone text NOT NULL,
  birthday text NOT NULL,
  mobile_carrier text,
  description text,
  status text DEFAULT '접수'::text,
  CONSTRAINT preorder_iphone17e_pkey PRIMARY KEY (id)
);
CREATE TABLE public.preorder_s26_orders (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text UNIQUE,
  birthday text,
  plan text,
  freebie text,
  installment text,
  model text,
  discount text,
  benefit text,
  petName text,
  installment_principal text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  profile_id uuid,
  kakao_friend_rewarded boolean DEFAULT false,
  kakao_friend_checked_at timestamp with time zone,
  CONSTRAINT preorder_s26_orders_pkey PRIMARY KEY (no),
  CONSTRAINT preorder_s26_orders_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.preorder_secret (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  event_id text,
  created_at timestamp with time zone NOT NULL DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  name text NOT NULL,
  phone text NOT NULL,
  model text,
  register_type text,
  is_processed boolean NOT NULL DEFAULT false,
  is_consultation boolean NOT NULL DEFAULT false,
  plan text,
  concern text,
  competitor text,
  current_carrier text,
  preorder_elsewhere text,
  CONSTRAINT preorder_secret_pkey PRIMARY KEY (id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text UNIQUE,
  full_name text,
  avatar_url text,
  provider text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  kakao_id text UNIQUE,
  naver_id text UNIQUE,
  last_login_at timestamp with time zone,
  phone text,
  birthday text,
  marketing_agreed boolean DEFAULT false,
  is_active boolean DEFAULT true,
  deleted_at timestamp with time zone,
  nickname text,
  CONSTRAINT profiles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.referral_consultation (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('asia/seoul'::text, now()),
  need_phone_consult boolean NOT NULL,
  need_internet_consult boolean NOT NULL,
  referrer_name text NOT NULL,
  referrer_phone text NOT NULL,
  buyer_name text,
  buyer_phone text,
  phone_model text,
  join_type text,
  current_carrier text,
  change_timing text,
  phone_inquiry text,
  current_internet text,
  internet_service text,
  install_region text,
  internet_inquiry text,
  privacy_agreed boolean NOT NULL,
  buyer_relation text,
  is_processed boolean NOT NULL DEFAULT false,
  is_self_purchase boolean,
  CONSTRAINT referral_consultation_pkey PRIMARY KEY (id)
);
CREATE TABLE public.restock_notification (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text UNIQUE,
  birthday text,
  plan text,
  freebie text,
  installment text,
  model text,
  discount text,
  benefit text,
  petName text,
  installment_principal text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  carrier text,
  CONSTRAINT restock_notification_pkey PRIMARY KEY (no)
);
CREATE TABLE public.reviews (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  rating integer NOT NULL DEFAULT 5,
  content text NOT NULL,
  image_url text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  image_urls ARRAY DEFAULT '{}'::text[],
  CONSTRAINT reviews_pkey PRIMARY KEY (id)
);
CREATE TABLE public.s26-color (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT (now() + '09:00:00'::interval),
  model text NOT NULL,
  color text NOT NULL,
  name text NOT NULL,
  phone text NOT NULL,
  is_processed boolean DEFAULT false,
  CONSTRAINT s26-color_pkey PRIMARY KEY (id)
);
CREATE TABLE public.s26_cancellations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  birthday text NOT NULL,
  phone text NOT NULL,
  device_model text NOT NULL,
  cancel_reason text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('Asia/Seoul'::text, now()),
  is_processed boolean NOT NULL DEFAULT false,
  CONSTRAINT s26_cancellations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.s26_customer_consultations (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  name text NOT NULL,
  model text NOT NULL,
  phone text NOT NULL,
  bank_name text NOT NULL,
  account_number text NOT NULL,
  description text,
  mobile_carrier text,
  is_processed boolean NOT NULL DEFAULT false,
  pet_name text,
  zip_code text,
  address text,
  detail_address text,
  CONSTRAINT s26_customer_consultations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.s26_orders (
  no bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  datetime timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  device text,
  capacity text,
  color text,
  register text,
  name text,
  phone text UNIQUE,
  birthday text,
  plan text,
  freebie text,
  installment text,
  model text,
  discount text,
  benefit text,
  petName text,
  installment_principal text,
  is_processed boolean DEFAULT false,
  is_consultation boolean DEFAULT false,
  carrier text,
  profile_id uuid,
  kakao_friend_rewarded boolean DEFAULT false,
  kakao_friend_checked_at timestamp with time zone,
  CONSTRAINT s26_orders_pkey PRIMARY KEY (no),
  CONSTRAINT s26_orders_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.user_viewed_devices (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  viewed_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  profile_id uuid NOT NULL,
  device_model text NOT NULL,
  CONSTRAINT user_viewed_devices_pkey PRIMARY KEY (id),
  CONSTRAINT user_viewed_devices_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id),
  CONSTRAINT user_viewed_devices_device_model_fkey FOREIGN KEY (device_model) REFERENCES public.devices(model)
);
CREATE TABLE public.user_wishlists (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  profile_id uuid NOT NULL,
  device_model text NOT NULL,
  CONSTRAINT user_wishlists_pkey PRIMARY KEY (id),
  CONSTRAINT user_wishlists_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id),
  CONSTRAINT user_wishlists_device_model_fkey FOREIGN KEY (device_model) REFERENCES public.devices(model)
);
CREATE TABLE public.usim_apply (
  id bigint NOT NULL DEFAULT nextval('usim_apply_id_seq'::regclass),
  carrier text NOT NULL,
  name text NOT NULL,
  birthday text NOT NULL,
  phone text NOT NULL,
  created_at timestamp with time zone DEFAULT (now() AT TIME ZONE 'Asia/Seoul'::text),
  is_processed boolean DEFAULT false,
  funnel text DEFAULT 'usim'::text,
  CONSTRAINT usim_apply_pkey PRIMARY KEY (id)
);
CREATE TABLE public.watch8_reservations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  name text NOT NULL,
  birthday text NOT NULL,
  phone text NOT NULL,
  is_processed boolean DEFAULT false,
  CONSTRAINT watch8_reservations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.watch_tablet (
  category text,
  model text NOT NULL,
  pet_name text,
  colors_kr ARRAY NOT NULL DEFAULT '{}'::text[],
  colors_en ARRAY NOT NULL DEFAULT '{}'::text[],
  capacities ARRAY NOT NULL DEFAULT '{}'::text[],
  paths ARRAY NOT NULL DEFAULT '{}'::text[],
  images jsonb DEFAULT '{}'::jsonb,
  colors_code ARRAY NOT NULL,
  company text,
  capacity text,
  form_link text,
  disclosure_subsidy integer,
  ktmakret_subsidy integer,
  price integer,
  isBundle boolean,
  isAvailable boolean,
  CONSTRAINT watch_tablet_pkey PRIMARY KEY (model)
);
CREATE TABLE public.watch_tablet_duplicate (
  category text,
  model text NOT NULL,
  pet_name text,
  colors_kr ARRAY NOT NULL DEFAULT '{}'::text[],
  colors_en ARRAY NOT NULL DEFAULT '{}'::text[],
  capacities ARRAY NOT NULL DEFAULT '{}'::text[],
  paths ARRAY NOT NULL DEFAULT '{}'::text[],
  images jsonb DEFAULT '{}'::jsonb,
  colors_code ARRAY NOT NULL,
  company text,
  capacity text,
  form_link text,
  disclosure_subsidy integer,
  ktmakret_subsidy integer,
  price integer,
  isBundle boolean,
  isAvailable boolean,
  CONSTRAINT watch_tablet_duplicate_pkey PRIMARY KEY (model)
);